import {
  LandingHeroSection,
  SocialProofMarqueeSection,
} from "../../components";

const Landing = () => {
  return (
    <main>
      <LandingHeroSection />
      <SocialProofMarqueeSection />
    </main>
  );
};

export default Landing;
